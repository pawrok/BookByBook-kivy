#:kivy 1.9

RootWidget:
    BoxLayout:
        orientation: 'vertical'
        size: root.width, root.height

        BoxLayout:
            canvas:
                Color:
                    rgb: .7, .3, .3
                Rectangle:
                    pos: self.pos
                    size: self.size
            size_hint_y: 0.1
            Label:
                text: "sth there"
